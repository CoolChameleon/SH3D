import torch

def sample_pixels(n_points_sampled: int=1024, img_res=[200, 200], mask=None, mask_ratio=None, view_idx_list=torch.tensor([0])):
    """
    if mask is None:
        sample pixels from 
    """
    pass